spring:
  application:
    name: demospringdata
  datasource:
    url: jdbc:postgresql://localhost:5432/springdata
    username: brucehan
    password: pass

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        format_sql: true
        show_sql: true